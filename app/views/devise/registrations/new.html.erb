<div class="content">

  <div id="sign">

  <h2>新規登録</h2>

  <div class="field">

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <h3 class="sns">SNSで登録</h3>
      <div class="sns_news">
        <div class="sns_new google">
        <%= link_to "Googleアカウントで新規登録", user_google_oauth2_omniauth_authorize_path %><br />
        </div>
        <div class="sns_new fb">
        <%= link_to "Facebookアカウントで新規登録", user_facebook_omniauth_authorize_path %><br />
        </div>
        <div class="sns_new twitter">
        <%= link_to "Twitterアカウントで新規登録", user_twitter_omniauth_authorize_path %><br />
        </div>
        <p><span class="small">新規登録するにあたっては、<a href="/terms" class="terms_agree">利用規約</a>に同意するものとします。<br>
        ユーザー登録できるのは20歳以上の方に限ります。</span></p>
      </div>

    <h3 class="mail_new">メールアドレスで登録</h3>

        <p><span class="data"><%= f.label :ニックネーム %></span></p>
        <%= f.text_field :nickname, placeholder: '  ニックネーム（最大20文字まで）',autofocus: true, maxlength: 20 %>



        <p><span class="data"><%= f.label :email %></span></p>
        <%= f.email_field :email, placeholder: '  メールアドレス', autocomplete: "email" %>


        <p><span class="data"><%= f.label :password %></span></p>

        <%= f.password_field :password, placeholder: '  パスワード', autocomplete: "new-password" %>
        <em><span class="small">(※半角英数字8文字以上、大文字・小文字・数字を1つ以上含める)</span></em>



        <p><span class="data"><%= f.label :年齢確認 %></span><span class="small">（※ユーザー登録できるのは２０歳以上の方に限ります)</span><br>
        <%= f.check_box :over20, {} , 1, nil %><span class="data">20歳以上</span></p>
        <p><%= f.check_box :agree, {} , 1, nil %><span class="data"><a href="/terms" class="terms_agree">利用規約</a>に同意する</span></p>


      <div class="actions">
        <%= f.submit "新規登録" %>
      </div>

      <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
        <%= link_to "確認メール再送", new_confirmation_path(resource_name) %><br />
      <% end %>

    <% end %>
  </div>
  </div>
</div>
